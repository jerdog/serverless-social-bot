{
  "name": "serverless-social-bot",
  "version": "1.0.0",
  "description": "A serverless social media bot that generates content using Markov chains",
  "type": "module",
  "main": "bot.js",
  "scripts": {
    "start": "node -r dotenv/config bot.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
